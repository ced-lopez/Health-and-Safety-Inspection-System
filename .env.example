# Root compose env - used for variable substitution in docker-compose.yml / docker-compose.prod.yml
# Copy to .env and set strong passwords before `docker compose up`
# This file is committed as example; real .env is gitignored (see .gitignore:4)

# ---- Database (shared by db + backend) ----
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=health_safety_inspection
DB_USERNAME=health_safety
DB_PASSWORD=change_me_strong_password
DB_ROOT_PASSWORD=change_me_strong_root_password

# ---- App (dev defaults) ----
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:5173,localhost:5174,127.0.0.1,127.0.0.1:5173,127.0.0.1:5174
VITE_API_URL=http://localhost:8000/api

# ---- Prod overrides (set in backend/.env.production instead) ----
# APP_URL=https://hsis.barangay178caloocan.com
# FRONTEND_URL=https://hsis.barangay178caloocan.com
# CORS_ALLOWED_ORIGINS=https://hsis.barangay178caloocan.com
# SANCTUM_STATEFUL_DOMAINS=hsis.barangay178caloocan.com
