server {
    listen 80;
    server_name hsis.barangay178caloocan.com;

    root /usr/share/nginx/html;
    index index.html;

    # ACME challenge for certbot
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # API proxy to backend
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /storage/ {
        proxy_pass http://backend:8000;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
}

# when TLS certs exist, add 443 server block:
# server {
#     listen 443 ssl;
#     server_name hsis.barangay178caloocan.com;
#     ssl_certificate /etc/letsencrypt/live/hsis.barangay178caloocan.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/hsis.barangay178caloocan.com/privkey.pem;
#     ... same locations ...
# }
